services:
  - type: web
    name: my-js-server
    env: node
    repo: https://github.com/ticia/Webserver
    branch: main
    buildCommand: "npm install"
    startCommand: "npm start"
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 18
